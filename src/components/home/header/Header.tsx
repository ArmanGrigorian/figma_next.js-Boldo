import { LeftSide } from "@/components";

const Header = () => {
	return (
		<header className="bg-custom-dark-blue min-h-[700px] px-10 max-md:px-5">
			<LeftSide />
		</header>
	);
};

export default Header;
