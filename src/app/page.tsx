import { Header, Section } from "@/components/home";

export default function HomePage() {
	return (
		<main className="max-w-[1440px] m-auto h-screen overflow-y-auto snap-mandatory snap-both">
			<Header />
			<Section />
		</main>
	);
}
